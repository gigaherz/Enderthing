buildscript {
    repositories {
        jcenter()
        maven {
            name = "forge"
            url = "http://files.minecraftforge.net/maven"
        }
    }
    dependencies {
        classpath 'net.minecraftforge.gradle:ForgeGradle:2.1-SNAPSHOT'
    }
}

repositories {
    maven {
        url "http://dvs1.progwml6.com/files/maven"
    }
}

apply plugin: 'net.minecraftforge.gradle.forge'

version = "0.5.0"
group= "gigaherz.enderthing" // http://maven.apache.org/guides/mini/guide-naming-conventions.html
archivesBaseName = "enderthing-1.8.9"

minecraft {
    version = "1.8.9-11.15.1.1855"
    runDir = "run"

    mappings = "stable_22"

    replace "@VERSION@", project.version
    replaceIn "Enderthing.java"

    useDepAts = true
}

dependencies {
    deobfCompile "mezz.jei:jei_1.8.9:2.28.17.185"
}

processResources
{
    inputs.property "version", project.version
    inputs.property "mcversion", project.minecraft.version

    from(sourceSets.main.resources.srcDirs) {
        include 'mcmod.info'
        expand 'version':project.version, 'mcversion':project.minecraft.version
    }
    from(sourceSets.main.resources.srcDirs) {
        exclude 'mcmod.info'
    }
}